# diylib 编译修复说明（v2 / miyou_features）

源码包：`diylib_v2_miyou_features.zip`，目标：微信(`com.tencent.xin`)，theos Tweak。

> **v26.0.2 更新（换包名，绕开旧缓存）**：包名由 `diylib.dylib` 改为 **`NPC.dylib`**（内部 `LC_ID_DYLIB` 同步改成 `NPC.dylib`），内部版本号 `26.0.1 → 26.0.2`。重新注入时若仍像旧版，多半是设备/下载器按**文件名**缓存了 `diylib.dylib`；换名后旧链接失效、强制拉新。装好后看日志：出现 `diylib v26.0.2 (NPC) by yZFAIU loaded into WeChat` 即确认加载的是新版（而非旧缓存）。

## 修复的编译漏洞

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 1 | `src/modules/*.m`、`src/ui/DPPreferencesListController.m` | `#import "../DPConfig.h"` 等相对路径错误：`DPConfig.h` 在**顶层**，而 `../DPConfig.h` 从 `src/modules/` 解析为不存在的 `src/DPConfig.h` | 全部改为裸名 `DPConfig.h` 等，并在 Makefile 增加 `-I./src -I./src/modules -I./src/ui` |
| 2 | `src/ui/DPPreferencesListController.m` (行 328/336) | `]};` 少一个 `]`，`[NSSet setWithArray:@[...]` 的消息括号未闭合 → 编译错误 | 改为 `] ];` |
| 3 | `src/DPMessageDispatcher.m` | `dispatchMessage:phase:` 在声明前被调用（私有方法无前向声明）→ 编译错误 | 在 `.m` 顶部加 class extension 前向声明 |
| 4 | `src/ui/DPPreferencesListController.h` | `DPPreferencesListController` 未声明遵循 `UITextFieldDelegate`，`tf.delegate = self` 报错 | `@interface ... : UITableViewController <UITableViewDelegate, UITableViewDataSource, UITextFieldDelegate>` |
| 5 | `src/modules/DisableFunctionHook.m` | `[self.navigationController ...]`：`self` 为 `id`，`navigationController` 是属性（非方法）→ 报错 | 改为 `[(UIViewController *)self navigationController]` |
| 6 | `src/modules/BackgroundKeepAlive.m` | 文件为 GBK 编码（586 个非 ASCII 字节） | `iconv -f GBK -t UTF-8` 转 UTF-8 |
| 7 | `Makefile` | theos 默认开启 `-Werror`，把 `performSelector` 泄漏告警升级为错误 | 在 `diylib_CFLAGS` 追加 `-Wno-error`（仅用于本次沙箱构建） |

## 构建方式（沙箱）

1. `theos make` 负责 Logos 预处理 + 逐文件编译（arm64 / arm64e 各 23 个 `.o`）。
2. theos 的链接步骤在沙箱失败（传给 `ld.lld` 的 `-iphoneos_version_min`、`-syslibroot`、substrate 路径等 macOS 风格参数不被识别）。
3. 手动用 `clang -target arm64-apple-darwin -fuse-ld=lld -dynamiclib -undefined dynamic_lookup` 链接每个架构的 `.o`，本环境 lld 直接产出**正确的小端 `0xfeedfacf` (MH_MAGIC_64)**，无需字节交换。
4. arm64e 切片的 `cpusubtype` 修正为 `0x2`（ARM64E），与 arm64（subtype 0）区分，满足 fat 唯一性。
5. 剥离 `LC_BUILD_VERSION`（`0x32`）：TrollFools 注入时 `ldid -S` 会在该 LC 上 SIGSEGV，剥离后兼容。
6. 手动拼接 fat（universal）Mach-O（沙箱无 `lipo`）。

## 交付物（`/workspace`）

- **`NPC.dylib`** —— 主交付物。arm64+arm64e fat，内部名 `NPC.dylib`，`LC_BUILD_VERSION` 已剥离，**用于 TrollFools 注入微信**。
- `NPC.with_buildversion.dylib` —— 同样 fat 但**保留** `LC_BUILD_VERSION`，用于 TrollStore 直装（CoreTrust 签名需要该 LC）。
- `NPC_source.zip` —— 完整源码（顶层目录 `NPC/`）。

> 旧版 `diylib.dylib` / `diylib.with_buildversion.dylib` / `diylib_v2_fixed_source.zip` 已删除，请勿再用旧链接，避免命中缓存装回老版本。

## 注入方式

- TrollFools：用 TrollFools 将 **`NPC.dylib`** 注入微信 IPA，重签安装即可。注入时删掉旧的 `diylib.dylib` 再添新的 `NPC.dylib`，避免两个 dylib 同时被加载。
- 总开关在微信「设置 → NPC」中开启；入口本身始终注入。
- 依赖运行时 Substrate/ElleKit（提供 `MSHookMessageEx`），注入环境需已具备。
- **确认未读旧缓存**：打开微信后看日志，出现 `diylib v26.0.2 (NPC) by yZFAIU loaded into WeChat` 即表示加载的是本次新包；若仍是 `v26.0.1` 说明还在读旧缓存（清掉旧 `diylib.dylib` 重新注入）。

---

## 设置入口修复（搬 `备份包.zip` / XJWeChatPay_v3.0 的方案）

用户反馈「设置页找不到插件设置入口」（微信 8.0.76）。根因是：微信「我」页（`NewSettingViewController` 等）的表格 **dataSource 委托给 `MMTableViewInfo` / `WCTableViewManager`**，并非 VC 自身 —— 只 hook VC 的表格方法（或在 VC 里给 section +1 行）都不会生效，且新版行高未知会渲染成 0 而不可见。先前多次尝试均卡在这一结构上。

本次**不再 hook 表格 dataSource**，而是**照搬用户上传 `备份包.zip` 里 XJWeChatPay 已验证的做法**：

### 做法
1. `%hook UIViewController - viewDidAppear:` —— 覆盖所有页面，但内部用类名白/黑名单过滤，**只对「主设置页」生效**。
2. `npc_isMainSettingsVC(cls)`：类名含 `Setting`，**且**不在排除列表（`Group/Chat/Room/Contact/Friend/Member/Privacy/Account/About/General/Notification/Message/Help/Label/Device/Security/Wallet`）里 → 判定为主设置页。这样自动覆盖 `SettingViewController` / `NewSettingViewController` 等各个版本的主设置页，且不会误伤子设置页。
3. `npc_injectSettingsHeader(vc)`：递归在 `vc.view` 子视图里找 `UITableView`（`npc_findTableView`，不依赖具体 ivar 名），把入口做成 **`tableView.tableHeaderView` 按钮**插到表格顶部。
   - `tableHeaderView` 一定渲染，与微信版本 / 数据源结构**完全无关**，也不存在「行高 0 不可见」的坑。
   - 按钮仿原生设置行样式：高度 48、左间距 16 的「NPC 设置」标题、右侧 `chevron.right`（SF Symbol，缺失时回退 `›`）、底部分隔线、点击轻微缩放反馈。
   - 用 `objc_setAssociatedObject(..., "npc_settings_injected")` 做幂等保护，只注入一次，不会重复。
4. 点击 → `UIViewController (NPCPluginSettings)` 分类的 `npc_openPluginSettingsAction` → `push` 到 `DPPreferencesListController`（该类已在 dylib 内定义，不会因类缺失崩溃）。入口**始终注入**，不受总开关控制，方便随时打开设置面板。

### 校验（已逐项通过）
- fat 结构合法：arm64(subtype 0) + arm64e(subtype 0x2)，双切片 inner magic 均为 `0xfeedfacf`。
- `NPC.dylib`（TrollFools 版）：两个切片**均无** `LC_BUILD_VERSION` → ldid 不再 SIGSEGV；`NPC.with_buildversion.dylib`（TrollStore 版）：**保留**该 LC 供 CoreTrust。内部 `LC_ID_DYLIB` 已为 `NPC.dylib`。
- 入口代码确实编入二进制（fat 双切片合计）：
  - `NPC 设置` 以 **UTF-16** 存入（字节 `N\x00P\x00C\x00 \x00\xbe\x8b\x6e\x7f`）✓
  - `setTableHeaderView:`、`viewDidAppear:`、`npc_injectSettingsHeader`、`npc_isMainSettingsVC`、`npc_openPluginSettings`、`npc_openPluginSettingsAction`、`npc_findTableView`、`npc_settings_injected`、`secondarySystemGroupedBackgroundColor`、`systemImageNamed`、`chevron.right`、`DPPreferencesListController` 均存在 ✓
  - **旧的脆弱方案已彻底移除**：`MMTableViewInfo` / `WCTableViewManager` / `NewSettingViewController` 字符串均**不存在** ✓
- 消息分发等其余 19 个功能模块（含 `CMessageMgr` / `AddMsg`）的代码保留不受影响。
- 注：含中文的 `@"..."` 常量被 clang 编译为 **UTF-16** 存入 `__cfstring`，用 UTF-8 字节 `grep` 会误判「字符串丢失」，须按 UTF-16 校验。
