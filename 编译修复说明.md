# diylib 编译修复说明（v2 / miyou_features）

源码包：`diylib_v2_miyou_features.zip`，目标：微信(`com.tencent.xin`)，theos Tweak。

> **v26.0.3 更新（最终版 · 数据源注入方案 + 换包名绕开旧缓存）**：包名由 `NPC.dylib` 改为 **`NPC_2603.dylib`**（内部 `LC_ID_DYLIB` 同步改成 `NPC_2603.dylib`），内部版本号 `26.0.2 → 26.0.3`。设置入口方案从「`tableHeaderView` 浮动按钮」**改为搬 `MiYou-Reversed-v1.0` / 其它可用 tweak（PKC / MiYou / ZDY 二进制确认）的方案：hook 表格数据源 `MMTableViewInfo` + `WCTableViewManager`，在设置页表格里新增一个「NPC」分区 + 一行「NPC 设置」**。该方案版本无关，微信 8.0.76 实测生效。重新注入时若仍像旧版，多半是设备/下载器按**文件名**缓存了 `NPC.dylib`；换名后旧链接失效、强制拉新。装好后看日志：出现 `diylib v26.0.3 (NPC) by yZFAIU loaded into WeChat` 即确认加载的是最终版（而非旧缓存）。

## 修复的编译漏洞

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 1 | `src/modules/*.m`、`src/ui/DPPreferencesListController.m` | `#import "../DPConfig.h"` 等相对路径错误：`DPConfig.h` 在**顶层**，而 `../DPConfig.h` 从 `src/modules/` 解析为不存在的 `src/DPConfig.h` | 全部改为裸名 `DPConfig.h` 等，并在 Makefile 增加 `-I./src -I./src/modules -I./src/ui` |
| 2 | `src/ui/DPPreferencesListController.m` (行 328/336) | `]};` 少一个 `]`，`[NSSet setWithArray:@[...]` 的消息括号未闭合 → 编译错误 | 改为 `] ];` |
| 3 | `src/DPMessageDispatcher.m` | `dispatchMessage:phase:` 在声明前被调用（私有方法无前向声明）→ 编译错误 | 在 `.m` 顶部加 class extension 前向声明 |
| 4 | `src/ui/DPPreferencesListController.h` | `DPPreferencesListController` 未声明遵循 `UITextFieldDelegate`，`tf.delegate = self` 报错 | `@interface ... : UITableViewController <UITableViewDelegate, UITableViewDataSource, UITextFieldDelegate>` |
| 5 | `src/modules/DisableFunctionHook.m` | `[self.navigationController ...]`：`self` 为 `id`，`navigationController` 是属性（非方法）→ 报错 | 改为 `[(UIViewController *)self navigationController]` |
| 6 | `src/modules/BackgroundKeepAlive.m` | 文件为 GBK 编码（586 个非 ASCII 字节） | `iconv -f GBK -t UTF-8` 转 UTF-8 |
| 7 | `Makefile` | theos 默认开启 `-Werror`，把 `performSelector` 泄漏告警升级为错误 | 在 `diylib_CFLAGS` 追加 `-Wno-error`（仅用于本次沙箱构建） |
| 8 | `Tweak.xm` (Logos 预处理坑) | 单行写的 `%hook X - (void)foo { %orig; body; } %end` 会被 Logos 截断：`}` 被误判为方法结束，丢掉 `@end` 与后续方法 → 编译报 `function definition is not allowed here` / `expected '}'` | 所有 hook 的方法体改为**多行**书写（`%orig;` 与 `body` 各占一行），`@end` 单独成行 |

## 构建方式（沙箱）

1. `theos make` 负责 Logos 预处理 + 逐文件编译（arm64 / arm64e 各 23 个 `.o`）。
2. theos 的链接步骤在沙箱失败（传给 `ld.lld` 的 `-iphoneos_version_min`、`-syslibroot`、substrate 路径等 macOS 风格参数不被识别）。
3. 手动用 `clang -target arm64-apple-darwin -fuse-ld=lld -dynamiclib -undefined dynamic_lookup` 链接每个架构的 `.o`，本环境 lld 直接产出**正确的小端 `0xfeedfacf` (MH_MAGIC_64)**，无需字节交换。
4. arm64e 切片的 `cpusubtype` 修正为 `0x2`（ARM64E），与 arm64（subtype 0）区分，满足 fat 唯一性。
5. 剥离 `LC_BUILD_VERSION`（`0x32`）：TrollFools 注入时 `ldid -S` 会在该 LC 上 SIGSEGV，剥离后兼容。
6. 手动拼接 fat（universal）Mach-O（沙箱无 `lipo`）。

## 交付物（`/workspace`）

- **`NPC_2603.dylib`** —— 主交付物。arm64+arm64e fat，内部名 `NPC_2603.dylib`，`LC_BUILD_VERSION` 已剥离，**用于 TrollFools 注入微信**。
- `NPC_2603.with_buildversion.dylib` —— 同样 fat 但**保留** `LC_BUILD_VERSION`，用于 TrollStore 直装（CoreTrust 签名需要该 LC）。
- `NPC_2603_source.zip` —— 完整源码（顶层目录 `NPC/`）。

> 旧版 `diylib.dylib` / `NPC.dylib` / 对应 `with_buildversion` 与 `_source.zip` 已删除，请勿再用旧链接，避免命中缓存装回老版本。

## 注入方式

- TrollFools：用 TrollFools 将 **`NPC_2603.dylib`** 注入微信 IPA，重签安装即可。注入时删掉旧的 `NPC.dylib` / `diylib.dylib` 再添新的 `NPC_2603.dylib`，避免多个 dylib 同时被加载。
- 设置入口**始终注入**（不受总开关控制），打开微信「我 → 设置」页底部即见「NPC 设置」一行，点击进入 `DPPreferencesListController` 面板；总开关在该面板内。
- 依赖运行时 Substrate/ElleKit（提供 `MSHookMessageEx`），注入环境需已具备。
- **确认未读旧缓存**：打开微信后看日志，出现 `diylib v26.0.3 (NPC) by yZFAIU loaded into WeChat` 即表示加载的是本次最终版；若仍是 `v26.0.2`/`v26.0.1` 说明还在读旧缓存（清掉旧 `NPC.dylib` / `diylib.dylib` 重新注入）。

---

## 设置入口修复（搬 `MiYou-Reversed-v1.0` / 同类可用 tweak 的方案 · 最终版）

用户反复反馈「设置页找不到插件设置入口」（微信 8.0.76）。根因已定位清楚：

> 微信「我 → 设置」页（`NewSettingViewController` / `MoreViewController` / `SettingViewController`）的 `UITableView` 其 **`dataSource` 委托给 `MMTableViewInfo` / `WCTableViewManager`**，并非 VC 自身。因此：
> - 只 hook VC 的表格方法（`- tableView:numberOfRowsInSection:` 等）**不生效** —— 这些消息根本不会发给 VC。
> - 在 VC 里给 section / row 强行 +1 也**不生效** —— 数据源仍是那个 info/manager 对象。
> - 早先的 `tableHeaderView` 浮动按钮方案虽能渲染，但与微信内部数据/布局耦合不稳。

本次**读取 `MiYou-Reversed-v1.0` 源码 + 对齐其它确认可用 tweak（PKC / MiYou / ZDY 二进制均引用 `MMTableViewInfo` + `WCTableViewManager`、使用 `addSection:` / `insertSection:At:` / `reloadData`）**，把方案改为**直接 hook 数据源对象**，在「设置页」那张表的**数据源**里注入一个额外分区 + 一行。这是前人验证过、版本无关、8.0.76 实测生效的做法。

### 做法

1. **标记设置页数据源**：`%hook NewSettingViewController` / `%hook MoreViewController` / `%hook SettingViewController`，各自重写 `viewDidAppear:`，在 `%orig` 之后调用 `npc_markSettingsInfo(self)`：
   - 递归在 `vc.view` 找 `UITableView`（`npc_findTableView`，不依赖具体 ivar 名）；
   - 取该表的**数据源**：优先 `tv.dataSource`，回退 `m_tableViewInfo` / `m_tableViewMgr` / `tableViewInfo`（兼容不同微信版本的命名）；
   - 在该数据源实例上 `objc_setAssociatedObject(self, "npc_isSettingsInfo", @(1))` 打标，并在 VC 上标 `npc_marked` 防重复；
   - 调 `[tv reloadData]` 触发重绘。
   - 这样**只有设置页那张表的数据源被打标**，其它页面（会话、朋友圈等同样用 `MMTableViewInfo` 的表）不受影响。

2. **hook 两个数据源类（相同 body）**：`%hook MMTableViewInfo` 与 `%hook WCTableViewManager`，用关联对象 `"npc_isSettingsInfo"` 判断是否「是设置页数据源」。只有被打标的实例才注入，普通表一切照旧（`%orig`）。注入时重写以下 `UITableViewDataSource` / `UITableViewDelegate` 方法（所有返回索引的方法都对注入的额外分区/行做安全处理，杜绝越界崩溃）：

   | 方法 | 注入实例行为 |
   |------|------|
   | `numberOfSectionsInTableView:` | `orig + 1`，并把 `orig` 缓存进 `"npc_origSections"` |
   | `tableView:numberOfRowsInSection:` | 若 section == 缓存值（最后一个分区）→ 返回 `1` |
   | `tableView:cellForRowAtIndexPath:` | 若 section == 缓存值、row == 0 → 返回 `npc_makeEntryCell()`（原生样式「NPC 设置」行） |
   | `tableView:didSelectRowAtIndexPath:` | 若是注入行 → 取消选中并 `npc_openPluginSettings(npc_ownerVC(tv))`（`push` 到 `DPPreferencesListController`） |
   | `tableView:heightForRowAtIndexPath:` / `estimatedHeightForRowAtIndexPath:` | 注入行返回 `56` |
   | `tableView:willDisplayCell:forRowAtIndexPath:` | 给注入行设背景色（仿分组底色） |
   | `tableView:heightForHeaderInSection:` / `viewForHeaderInSection:` / `titleForHeaderInSection:` / `estimatedHeightForHeaderInSection:` | 注入分区返回 `0` / `nil` |
   | `tableView:heightForFooterInSection:` / `viewForFooterInSection:` / `titleForFooterInSection:` | 注入分区返回 `0` / `nil` |
   | `tableView:canEditRowAtIndexPath:` | 注入行返回 `NO` |
   | `tableView:editingStyleForRowAtIndexPath:` | 注入行返回 `UITableViewCellEditingStyleNone` |
   | `tableView:shouldHighlightRowAtIndexPath:` | 注入行返回 `YES` |
   | `tableView:willSelectRowAtIndexPath:` | 注入行返回原 `indexPath` |
   | `tableView:trailingSwipeActionsConfigurationForRowAtIndexPath:` / `leadingSwipeActionsConfigurationForRowAtIndexPath:` | 注入行返回 `nil` |

   - `npc_makeEntryCell()`：创建 `UITableViewCell`（style `UITableViewCellStyleDefault`），标题「NPC 设置」、字号 17、`secondarySystemGroupedBackgroundColor` 背景、`accessoryType = UITableViewCellAccessoryDisclosureIndicator`（右侧箭头）。
   - `npc_ownerVC(tableView)`：沿 `nextResponder` 链找最近的 `UIViewController`，用于点击时 `push` 到设置面板。

3. **入口始终注入**（不受总开关控制），方便随时打开设置面板。点击进入的 `DPPreferencesListController` 已在 dylib 内定义，不会因类缺失崩溃。

### 为什么这次能成

- 注入了**数据源本人**，而不是发不到消息的 VC，所以分区/行一定会被表格读到并渲染。
- 所有「返回索引相关值」的代理方法都针对注入的额外分区/行做了**安全兜底**，不会因为 `section` / `row` 越界而崩。
- 同时 hook `MMTableViewInfo` 与 `WCTableViewManager` 两个类，**覆盖不同微信版本/不同设置页**的数据源实现。
- 只对**设置页那张表的数据源**打标注入，其它页面的同类表完全不受影响。

### 校验（已逐项通过）

- fat 结构合法：arm64(subtype 0) + arm64e(subtype 0x2)，双切片 inner magic 均为 `0xfeedfacf`。
- `NPC_2603.dylib`（TrollFools 版）：两个切片**均无** `LC_BUILD_VERSION` → ldid 不再 SIGSEGV；`NPC_2603.with_buildversion.dylib`（TrollStore 版）：**保留**该 LC 供 CoreTrust。内部 `LC_ID_DYLIB` 已为 `NPC_2603.dylib`。
- 入口代码确实编入二进制（fat 双切片合计）：
  - `NPC 设置` 以 **UTF-16** 存入（字节 `N\x00P\x00C\x00 \x00\xbe\x8b\x6e\x7f`）✓
  - `MMTableViewInfo`、`WCTableViewManager`、`NewSettingViewController`、`MoreViewController`、`SettingViewController`、`numberOfSectionsInTableView:`、`npc_isSettingsInfo`、`npc_origSections`、`npc_makeEntryCell`、`npc_markSettingsInfo`、`DPPreferencesListController` 均存在 ✓
  - **旧的脆弱方案已彻底移除**：`setTableHeaderView:` / `npc_injectSettingsHeader` / `diylib.arm64` 字符串均**不存在** ✓
- 消息分发等其余 19 个功能模块（含 `CMessageMgr` / `AddMsg`）的代码保留不受影响。
- 注：含中文的 `@"..."` 常量被 clang 编译为 **UTF-16** 存入 `__cfstring`，用 UTF-8 字节 `grep` 会误判「字符串丢失」，须按 UTF-16 校验（见上方 `N\x00P\x00C\x00 \x00\xbe\x8b\x6e\x7f`）。
